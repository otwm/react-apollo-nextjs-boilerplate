query getTracks {
  tracksForHome {
    id
    title
    thumbnail
    length
    modulesCount
    author {
      name
      photo
    }
  }
}

query getSimpleTrack {
  tracksForHome {
    id
    title
  }
}